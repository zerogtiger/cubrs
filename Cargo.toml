[package]
name = "cube_timer"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = "1.25.0"
crossterm = "0.29.0"
rand = "0.10.1"
rand_chacha = "0.10.0"

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "solver_benchmark"
harness = false

